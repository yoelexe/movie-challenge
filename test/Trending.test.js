import '@testing-library/jest-dom/extend-expect'

//? Types of tests
//? Unit tests
//? Integration tests
//? E2E tests
// renderizar un componente e 
// inspeccionar que es lo que ha renderizado
/* import { render } from "@testing-library/react";
import Trending from '../src/components/home/trending/Trending' */
/* import { BrowserRouter } from 'react-router-dom'; */

/* describe('testing', () => {
  it('render',  () => {
    const trending = {
      content: 'This is a test',
      important: true
    }
  
    const component = render(<Trending trending={trending}/>)
    console.log(component)
  }) 
}) */

/* test('renders Trending', async () => {
  render(
    <BrowserRouter>
    <Trending />
    </BrowserRouter>
  )

  await waitFor(() => screen.getByText("Now In Cinemas"));

  expect(screen.getByText("Now In Cinemas")).toBeInTheDocument();
}) */
